import "../LogOut/Logout.css";
const Logout = () => {
  return (
    <div>
      <button className="cta flex justify-center">
        <span>Logout</span>
      </button>
    </div>
  );
};

export default Logout;
